export default function HighProfileAchievementWebsite() {
  const achievements = [
    {
      title: 'Global Leadership Award',
      year: '2025',
      description:
        'Recognized for transformational leadership and strategic innovation across international markets.',
    },
    {
      title: 'Featured Keynote Speaker',
      year: '2024',
      description:
        'Delivered keynote presentations at major conferences and executive summits.',
    },
    {
      title: 'Industry Excellence Recognition',
      year: '2023',
      description:
        'Honored for exceptional contributions, growth initiatives, and measurable impact.',
    },
  ];

  const milestones = [
    {
      metric: '15+',
      label: 'Years of Excellence',
    },
    {
      metric: '50+',
      label: 'Projects Led',
    },
    {
      metric: '20+',
      label: 'Global Partnerships',
    },
    {
      metric: '$100M+',
      label: 'Business Impact',
    },
  ];

  return (
    <div className="min-h-screen bg-black text-white font-sans">
      {/* Hero Section */}
      <section className="relative overflow-hidden border-b border-white/10">
        <div className="absolute inset-0 bg-gradient-to-br from-zinc-900 via-black to-zinc-950 opacity-90" />

        <div className="relative max-w-7xl mx-auto px-6 py-28 lg:py-36 grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <p className="uppercase tracking-[0.35em] text-sm text-zinc-400 mb-6">
              Executive Portfolio
            </p>

            <h1 className="text-5xl md:text-7xl font-black leading-tight tracking-tight mb-8">
              Your Name
            </h1>

            <p className="text-xl text-zinc-300 leading-relaxed max-w-2xl mb-10">
              Visionary leader, innovator, and high-impact professional recognized
              for delivering exceptional results, building influential
              partnerships, and driving meaningful transformation across
              industries.
            </p>

            <div className="flex flex-wrap gap-4">
              <button className="px-8 py-4 rounded-2xl bg-white text-black font-semibold hover:scale-105 transition-transform duration-300">
                View Achievements
              </button>

              <button className="px-8 py-4 rounded-2xl border border-white/20 hover:bg-white/10 transition duration-300">
                Contact
              </button>
            </div>
          </div>

          <div className="relative">
            <div className="absolute -inset-8 bg-white/5 blur-3xl rounded-full" />

            <div className="relative bg-white/5 border border-white/10 backdrop-blur-xl rounded-[2rem] p-8 shadow-2xl">
              <div className="aspect-square rounded-[1.5rem] bg-gradient-to-br from-zinc-800 to-zinc-900 flex items-center justify-center text-zinc-500 text-lg border border-white/10">
                Professional Portrait Placeholder
              </div>

              <div className="grid grid-cols-2 gap-4 mt-6">
                {milestones.map((item, index) => (
                  <div
                    key={index}
                    className="rounded-2xl bg-black/40 border border-white/10 p-5"
                  >
                    <div className="text-3xl font-bold mb-2">{item.metric}</div>
                    <div className="text-zinc-400 text-sm">{item.label}</div>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* About Section */}
      <section className="max-w-6xl mx-auto px-6 py-24">
        <div className="grid lg:grid-cols-2 gap-16 items-start">
          <div>
            <p className="uppercase tracking-[0.3em] text-sm text-zinc-500 mb-4">
              About
            </p>

            <h2 className="text-4xl md:text-5xl font-bold mb-8 leading-tight">
              Building influence through excellence and innovation.
            </h2>
          </div>

          <div className="space-y-6 text-lg text-zinc-300 leading-relaxed">
            <p>
              Replace this section with your biography, career highlights, and
              mission statement. Showcase your leadership style, personal story,
              and the value you bring to organizations and communities.
            </p>

            <p>
              This premium portfolio layout is designed to position you as a
              world-class professional with a refined digital presence.
            </p>
          </div>
        </div>
      </section>

      {/* Achievements Section */}
      <section className="bg-zinc-950 border-y border-white/10 py-24 px-6">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-16">
            <div>
              <p className="uppercase tracking-[0.3em] text-sm text-zinc-500 mb-4">
                Recognition
              </p>

              <h2 className="text-4xl md:text-5xl font-bold">
                Career Achievements
              </h2>
            </div>

            <p className="text-zinc-400 max-w-xl text-lg">
              Highlight the milestones, awards, partnerships, and measurable
              successes that define your professional legacy.
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-8">
            {achievements.map((achievement, index) => (
              <div
                key={index}
                className="rounded-[2rem] border border-white/10 bg-white/5 p-8 hover:-translate-y-2 transition-transform duration-300"
              >
                <div className="flex items-center justify-between mb-6">
                  <span className="text-zinc-500 text-sm tracking-wide uppercase">
                    Achievement
                  </span>

                  <span className="text-white font-semibold">
                    {achievement.year}
                  </span>
                </div>

                <h3 className="text-2xl font-bold mb-4">
                  {achievement.title}
                </h3>

                <p className="text-zinc-400 leading-relaxed">
                  {achievement.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Timeline */}
      <section className="max-w-5xl mx-auto px-6 py-24">
        <div className="mb-16">
          <p className="uppercase tracking-[0.3em] text-sm text-zinc-500 mb-4">
            Journey
          </p>

          <h2 className="text-4xl md:text-5xl font-bold">
            Professional Timeline
          </h2>
        </div>

        <div className="space-y-10 border-l border-white/10 ml-3">
          {[
            {
              year: '2026',
              title: 'Executive Leadership Expansion',
              desc: 'Led strategic initiatives with global impact and measurable growth.',
            },
            {
              year: '2024',
              title: 'International Recognition',
              desc: 'Received prestigious recognition for innovation and influence.',
            },
            {
              year: '2021',
              title: 'Major Business Milestone',
              desc: 'Successfully launched and scaled transformative projects.',
            },
          ].map((item, index) => (
            <div key={index} className="relative pl-10">
              <div className="absolute -left-[11px] top-2 w-5 h-5 rounded-full bg-white" />

              <div className="text-zinc-500 text-sm mb-2">{item.year}</div>

              <h3 className="text-2xl font-semibold mb-3">{item.title}</h3>

              <p className="text-zinc-400 text-lg leading-relaxed">
                {item.desc}
              </p>
            </div>
          ))}
        </div>
      </section>

      {/* Testimonials */}
      <section className="bg-white text-black py-24 px-6">
        <div className="max-w-6xl mx-auto text-center">
          <p className="uppercase tracking-[0.3em] text-sm text-zinc-500 mb-4">
            Reputation
          </p>

          <h2 className="text-4xl md:text-5xl font-bold mb-16">
            What People Say
          </h2>

          <div className="grid md:grid-cols-3 gap-8 text-left">
            {[
              'An exceptional visionary whose leadership consistently drives extraordinary results.',
              'A trusted expert with unmatched professionalism and strategic insight.',
              'Highly respected for innovation, execution, and long-term impact.',
            ].map((quote, index) => (
              <div
                key={index}
                className="rounded-[2rem] border border-black/10 bg-zinc-100 p-8"
              >
                <p className="text-lg leading-relaxed mb-8">“{quote}”</p>

                <div>
                  <div className="font-semibold">Executive Partner</div>
                  <div className="text-zinc-500 text-sm">
                    Global Organization
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Contact */}
      <section className="max-w-5xl mx-auto px-6 py-24 text-center">
        <p className="uppercase tracking-[0.3em] text-sm text-zinc-500 mb-4">
          Connect
        </p>

        <h2 className="text-5xl font-black mb-8 leading-tight">
          Ready to collaborate on impactful opportunities.
        </h2>

        <p className="text-zinc-400 text-xl max-w-3xl mx-auto mb-10 leading-relaxed">
          Add your email address, LinkedIn profile, company information, and
          social channels to make networking effortless.
        </p>

        <div className="flex flex-wrap justify-center gap-4">
          <button className="px-8 py-4 rounded-2xl bg-white text-black font-semibold hover:scale-105 transition-transform duration-300">
            Email Me
          </button>

          <button className="px-8 py-4 rounded-2xl border border-white/10 hover:bg-white/5 transition duration-300">
            LinkedIn
          </button>
        </div>
      </section>
    </div>
  );
}
